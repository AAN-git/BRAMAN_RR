# Confidential: the dealer brand guidelines live here, not in the repository.
DO NOTO GIT/
DO NOTO GIT /

# The supplied masters (115MB). assets/video/ carries the web encodes.
VIDEOS/

.DS_Store
.playwright-mcp/
dist/
BRAMAN_RR-preview.zip
BRAMAN_RR-handoff-SL11.zip

# Reference screenshots dropped into the folder
*.png
!assets/**/*.png

# Agent skills installed into the project (npx skills add …) — tooling, not the site
.agents/
.claude/
skills-lock.json
scratch/

# The client's supplied masters — web encodes live in assets/img/
NEW IMAGES do not git/
